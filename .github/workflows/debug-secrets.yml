name: Debug Secrets (DELETE THIS FILE IMMEDIATELY AFTER USE!)

on:
  workflow_dispatch:

jobs:
  debug-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Print DOTENV secret
        run: |
          echo "=== DOTENV SECRET CONTENT BELOW ==="
          echo "${{ secrets.DOTENV }}"
          echo "=== DOTENV SECRET CONTENT ABOVE ===" 
